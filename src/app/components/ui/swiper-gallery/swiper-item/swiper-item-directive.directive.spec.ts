import { SwiperItemDirectiveDirective } from './swiper-item-directive.directive';

describe('SwiperItemDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SwiperItemDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
